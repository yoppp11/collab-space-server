"""
Pytest fixtures for workspaces app tests.

Note: Common fixtures (workspace, board, etc.) are defined in the root conftest.py
and are available to all tests.
"""
import pytest

# App-specific fixtures can be added here if needed
