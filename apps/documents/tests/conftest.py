"""
Pytest fixtures for documents app tests.

Note: Common fixtures (document, comment, etc.) are defined in the root conftest.py
and are available to all tests.
"""
import pytest

# App-specific fixtures can be added here if needed
